.pio/
.pioenvs/
.piolibdeps/
.vscode/
*.bin
*.elf
# webflash ships prebuilt flash images on purpose — the whole point of the page
!webflash/firmware/*.bin

node_modules/
.vercel/

# secrets live in NVS, never in the repo
.env
.env.*
*secret*
*credentials*.json
token.json

.DS_Store
Thumbs.db
